"""Pure-Python calculation engine for SubCalc.

This package has no dependency on Sublime Text's ``sublime``/``sublime_plugin``
modules, so it can be exercised directly with ``pytest`` outside the editor.
"""
